apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: web
  namespace: default
spec:
  hosts:
  - "instanaRobotshop.com"
  gateways:
  - web-gateway
  http:
  - route:
    - destination:
        host: web-svc
        port:
          number: 8080