{{- if .Values.services.ratings.user.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.services.ratings.name }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.services.ratings.name }}
  minReplicas: {{ .Values.services.ratings.user.minReplicas }}
  maxReplicas: {{ .Values.services.ratings.user.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.ratings.user.targetCPUUtilizationPercentage }}
{{- end }}
