{{- if .Values.services.user.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.services.user.name }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.services.user.name }}
  minReplicas: {{ .Values.services.user.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.services.user.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.user.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
