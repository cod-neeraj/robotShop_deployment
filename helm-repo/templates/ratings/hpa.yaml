{{- if .Values.services.ratings.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.services.ratings.name }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.services.ratings.name }}
  minReplicas: {{ .Values.services.ratings.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.services.ratings.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.ratings.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
