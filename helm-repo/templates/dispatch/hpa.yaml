{{- if .Values.services.dispatch.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.services.dispatch.name }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.services.dispatch.name }}
  minReplicas: {{ .Values.services.dispatch.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.services.dispatch.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.dispatch.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
