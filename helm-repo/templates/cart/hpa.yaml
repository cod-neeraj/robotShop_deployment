{{- if .Values.services.cart.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.services.cart.name }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.services.cart.name }}
  minReplicas: {{ .Values.services.cart.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.services.cart.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.cart.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
