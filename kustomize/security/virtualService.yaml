apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: robotshop
spec:
  hosts:
  - "*"
  gateways:
  - robotshop-gateway
  http:
  - match:
    - uri:
        prefix: "/"
    route:
    - destination:
        host: web-service-dev
        port:
          number: 8080